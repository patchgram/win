# Flutter / Dart build + tooling artifacts (regenerated by `flutter create` / `flutter build`).
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
build/
windows/        # native runner — regenerate with: flutter create --platforms=windows .
pubspec.lock
.packages
